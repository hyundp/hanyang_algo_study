탐색(search): 많은 양의 데이터 중에서 원하는 것을 찾는 과정
자료구조(data structure): 데이터를 표현, 관리, 처리하기 위한 구조
* Overflow: 특정 자료구조가 수용 가능한 데이터의 크기를 이미 가득 찬 상태에서 삽입 연산을 수행할 때 발생
* Underflow: 데이터가 전혀 들어있지 않은 상태에서 삭제 연산을 수행할 때 발생

스택(stack): 박스를 쌓는 것처럼 데이터를 추가하는 방법. 선입후출(First in, Last out) 또는 후입선출(Last in, First out) 구조라고 함.
큐(queue): 대기 줄처럼 데이터를 관리하는 방법. 선입선출(First in, First out) 구조라고 함.

재귀함수(Recursive function): 자신을 다시 호출하는 함수
-> 재귀 함수는 종료 조건을 명시해야 함. 그렇지 않으면 무한 호출되어 오류가 남. 

DFS(Depth-First Search): 깊이 우선 탐색
+동작 과정
 1. 탐색 시작 노드를 스택에 삽입, 방문 처리
 2. 최상단 노드에 방문하지 않은 인접 노드가 있으면 그것을 스택에 넣고 방문 처리, 방문하지 않은 인접 노드가 없으면 스택에서 최상단 노드를 꺼냄.
 3. 2의 과정을 더 수행할 수 없을 때까지 반복.
 
 
BFS(Breadth-First Search): 너비 우선 탐색
+동작 과정
 1. 탐색 시작 노드를 큐에 삽입하고 방문 처리
 2. 큐에서 노드를 꺼내 해당 노드의 인접 노드 중에서 방문하지 않은 노드를 모두 큐에 삽입하고 방문 처리
 3. 2의 과정을 더 수행할 수 없을 때까지 반복. 
 
*일반적인 경우는 DFS보다 BFS 구현이 조금 더 빠르게 동작함. 

