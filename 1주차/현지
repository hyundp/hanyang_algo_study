#파이썬 문법 정리

-	리스트 자료형 : 배열 혹은 테이블
비어있는 리스트 선언은 list()
*인덱스는 0부터 시작함*
Ex) a = [1,2,3,4,5]   //데이터를 넣어 리스트 초기화
   Print(a[3])    //이는 네 번째 원소를 출력하는 것
리스트 컴프리헨션은 2차원 리스트 초기화시 효과적

리스트 인덱싱 : 리스트의 특정한 원소에 접근
리스트 슬라이싱 : 연속적인 위치를 갖는 원소들을 가져올 때 (:) 를 이용하여 시작 인덱스와 끝 인덱스 설정 가능. 끝 인덱스는 실제 인덱스보다 1을 더 크게 설정

리스트 관련 메소드
append()   //요소 추가
sort()     //정렬
insert(인덱스, 값)   //특정한 인덱스 위치에 원소 삽입 
count()    //특정한 겂을 가지는 데이터의 개수 세기
remove()   //특정한 값 갖는 원소 제거.

-	튜플 자료형과 리스트의 차이
튜플은 한번 선언된 값은 변경할 수 없음
리스트는 대괄호([])를 이용하지만 튜플은 소괄호(( )) 이용
튜플은 리스트보다 메모리를 효율적으로 사용 가능

-	사전 자료형 : 키 (key)와 값(value)의 쌍을 데이터로 가지는 자료형
Ex) data = dict()
    data[‘사과’] = Apple’
    
사전 자료형 관련 메소드
Keys() 함수 : 키 데이터만 뽑아서 리스트로 이용
Values() 함수 : 값 데이터만 뽑아서 리스트로 이용

